# Example 1: single-package crate (uses [package])
[package]
name = "my-crate"
version = "1.3.0-dev4"
edition = "2021"

# Example 2: workspace crate (uses [workspace.package] — takes priority)
[workspace]
members = ["crate-a", "crate-b"]

[workspace.package]
version = "1.3.0-dev4"
edition = "2021"
